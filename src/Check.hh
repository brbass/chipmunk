#ifndef Check_hh
#define Check_hh

#include <vector>
#include <string>

using namespace std;

unsigned check(unsigned size,
               vector<double> &check_vector,
               string vector_name);

unsigned check(unsigned size,
               vector<string> &check_vector,
               string vector_name);

#endif
